import '../styles.css';

export default function Nextra({
  Component,
  pageProps: { session, ...pageProps },
}) {
  return (
    <div className='bg-blue'>
      <Component {...pageProps} />
    </div>
  );
}
